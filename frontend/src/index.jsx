import React from 'react';
import ReactDOM from 'react-dom/client';
import BatteryData from './BatteryData.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <BatteryData />
    </React.StrictMode>
);
